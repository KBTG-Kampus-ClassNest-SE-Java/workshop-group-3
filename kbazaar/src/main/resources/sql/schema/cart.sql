CREATE TABLE IF NOT EXISTS cart
(
    cart_id
    SERIAL
    PRIMARY
    KEY,
    quantity
    INT
    NOT
    NULL,
    username
    VARCHAR(255)
    NOT
    NULL,
    product_id
    INT
    NOT
    NULL
);